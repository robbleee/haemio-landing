export const metadata = {
  title: 'Our Vision — Haem.io',
  description: 'Advancing precision haematology through AI-driven diagnostic intelligence, transparency, and actionable clinical insights.',
};

export default function VisionLayout({ children }) {
  return children;
}
