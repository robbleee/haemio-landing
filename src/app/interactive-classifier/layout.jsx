export const metadata = {
  title: 'Interactive AML/MDS Classifier — Haem.io',
  description: 'Try the Haem.io classifier: WHO 2022 and ICC 2022 classification with ELN risk stratification. No login required.',
};

export default function ClassifierLayout({ children }) {
  return children;
}
